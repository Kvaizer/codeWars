function getMiddle(s){
  console.log(s);
  let arr = s.split('');
  let result = '';
  result = (arr.length % 2 === 0) ? `${arr[arr.length / 2 - 1]}${arr[arr.length / 2]}` : `${arr[Math.ceil(arr.length / 2) - 1]}`
  return result;
}
